<h1><%= @store.store_name %></h1>

<h3>View brands this store carries</h3>

<% if @store.brands.any? %>
  <h4>Here are your brands:</h4>

  <ul>
    <% @stores.brands.each do |brand| %>
      <li><a href="/stores/<%= brand.id()%>"><%= brand.brand_name %></a></li>
    <% end %>
  </ul>

<% else %>
  <h4>No brands added yet!</p>

<% end %>

<h2>Add a new brand:</h2>
<form action="/stores/" method="post">
  <input type="text" name="brand_name">
  <button type="submit">Add Brand</button>
</form>
